# Glow by BVY — Jewelry Demo

Quick start:

    npm install
    npm run dev

Then open the local URL it prints (usually http://localhost:5173).

To get a live shareable link, deploy the `dist/` output (after `npm run build`)
to Vercel, Netlify, or GitHub Pages — see the chat for step-by-step options.
